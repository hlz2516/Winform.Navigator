路由配置思路

由用户配置的变量：
所有页面所在的命名空间（可为空，即默认在当前运行exe中寻找所有的页面类）
是否开启鉴权（可为空，默认不开启）
路由：
path:路径，在这里配置后必须在对应页面类上写上相同路径的path特性，该配置不可为空，为空则报错
name:路由名，也是路径别名，该项也可配置在path特性上，若xml配置和特性均存在该项，则取特性上的。可为空。
cache：设置页面是否缓存。可为空，默认缓存。

实现该思路的必须技术及验证：
1. 给页面类加特性
2. 在指定命名空间中找到有path特性的页面类

已验证可以实现